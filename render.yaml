services:
  - type: web
    name: controle-campo-v2
    env: node
    buildCommand: npm ci --prefix backend
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
      - key: DATABASE_URL
        sync: false
